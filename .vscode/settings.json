// 将设置放入此文件中以覆盖默认值和用户设置。
{
    "saveAndRun": {
        "commands": [
            {
                "match": ".*",
                "cmd": "bash --login -c 'GEM_HOME=/home/c0ldcat/.gem/ruby/2.4.0 bundle exec jekyll build -I -d /srv/blog'"
            }
        ]
    }
}